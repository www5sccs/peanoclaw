Packed-Type: short int;

#include "peano/utils/Globals.h"

Constant: DIMENSIONS;
Constant: DIMENSIONS_TIMES_TWO;

/**
 * This class is generated via DaStGen.
 */
class peanoclaw::statistics::LevelStatistics {
  parallelise persistent double area;
  parallelise persistent int    level;
  parallelise persistent double numberOfPatches;
  parallelise persistent double numberOfCells;
  parallelise persistent double numberOfCellUpdates;
  parallelise persistent double createdPatches;
  parallelise persistent double destroyedPatches;
  parallelise persistent double patchesBlockedDueToNeighbors;
  parallelise persistent double patchesBlockedDueToGlobalTimestep;
  parallelise persistent double patchesSkippingIteration;
  parallelise persistent double patchesCoarsening;
  parallelise persistent int    estimatedNumberOfRemainingIterationsToGlobalTimestep;
  parallelise persistent double averageTimestepSize;
  parallelise persistent double minimalTimestepSize; 
};